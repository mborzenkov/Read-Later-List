<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mborzenkov.readlaterlist">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.MainList"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.EditItem"
            android:label="@string/title_activity_edit_item"
            android:launchMode="singleTop"
            android:parentActivityName=".Activity.MainList"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.mborzenkov.readlaterlist.Activity.MainList" />
        </activity>
        <activity android:name=".Activity.ColorPicker"
            android:label="@string/title_activity_edit_item"
            android:launchMode="singleTop"
            android:parentActivityName=".Activity.EditItem" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.mborzenkov.readlaterlist.Activity.EditItem" />
        </activity>

        <provider
            android:name=".data.ReadLaterContentProvider"
            android:authorities="@string/content_authority"
            android:exported="false"/>
    </application>

    <uses-permission android:name="android.permission.VIBRATE"/>

</manifest>